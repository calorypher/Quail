<Window
    x:Class="Quail.M08.WinUi.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quail.M08.WinUi">
  <Grid x:Name="RootGrid" Padding="16" Background="{ThemeResource LayerFillColorDefaultBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="12" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="12,8">
      <Grid ColumnSpacing="10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <FontIcon Glyph="&#xE721;" FontFamily="Segoe Fluent Icons" FontSize="16" VerticalAlignment="Center" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
        <TextBox x:Name="QueryBox" Grid.Column="1" BorderThickness="0" Background="Transparent" PlaceholderText="Search files" FontSize="18" Padding="0" TextChanged="OnQueryChanged" KeyDown="OnQueryKeyDown" />
        <TextBlock Grid.Column="2" Text="Esc" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorTertiaryBrush}" FontSize="12" />
      </Grid>
    </Border>

    <ListView x:Name="ResultsList" Grid.Row="2" SelectionMode="Single" IsItemClickEnabled="False" SelectionChanged="OnSelectionChanged" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
      <ListView.ItemContainerStyle>
        <Style TargetType="ListViewItem">
          <Setter Property="HorizontalContentAlignment" Value="Stretch" />
          <Setter Property="Padding" Value="0" />
          <Setter Property="MinHeight" Value="44" />
          <Setter Property="Margin" Value="0,1" />
        </Style>
      </ListView.ItemContainerStyle>
      <ListView.ItemTemplate>
        <DataTemplate>
          <Grid Height="44" Padding="10,5" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="24" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid Width="20" Height="20" VerticalAlignment="Center">
              <FontIcon Glyph="{Binding IconGlyph}" FontFamily="Segoe Fluent Icons" FontSize="18" VerticalAlignment="Center" />
              <Image Source="{Binding IconSource}" Stretch="Uniform" />
            </Grid>
            <StackPanel Grid.Column="1" Spacing="1" VerticalAlignment="Center">
              <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" FontSize="14" />
              <TextBlock Text="{Binding Path}" TextTrimming="CharacterEllipsis" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
            </StackPanel>
            <TextBlock Grid.Column="2" Text="{Binding Metadata}" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

    <TextBlock x:Name="StatusText" Grid.Row="3" Text="Ctrl+Alt+Space to show Quick Search" Foreground="{ThemeResource TextFillColorTertiaryBrush}" FontSize="12" />
  </Grid>
</Window>
